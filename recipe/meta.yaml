{% set name = "tqdm-loggable" %}
{% set version = "0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/tqdm_loggable-{{ version }}.tar.gz
  sha256: 083dcf7df7ea655d2d3bebf43fbf1593f5e334c2640964ea7bb0699cb7d34179

build:
  entry_points:
    - manual-tests = tqdm_loggable.manual_tests:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }}
    - tqdm >4.64

test:
  imports:
    - tqdm_loggable
  commands:
    - pip check
    - manual-tests --help
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/tradingstrategy-ai/tqdm-loggable
  summary: TQDM progress bar helpers for logging and other headless application
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - coroa
